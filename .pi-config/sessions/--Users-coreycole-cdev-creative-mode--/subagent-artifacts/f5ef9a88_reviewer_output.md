[P1] `harness/internal/swarmorch/agent.go:160` - `cmd.Wait()` is called before handling `loopErr`; when `readAgentLoop` exits early (e.g., tool-call limit at line 359), the child process is not terminated first. **Impact:** workflows can hang indefinitely waiting on a still-running agent process, tying up Temporal workers and leaving tasks stuck. **Fix suggestion:** on `loopErr`, explicitly close stdin and/or kill the process (`_ = cmd.Process.Kill()`) before `Wait`, then collect stderr.

[P1] `harness/internal/server/server.go:167` + `harness/internal/server/server.go:698` - swarm task-start/cancel/list API is protected by `hookSecretMiddleware`, but middleware is fail-open when `CM_HOOK_SECRET` is unset. **Impact:** accidental misconfig exposes unauthenticated endpoints that can spawn expensive Temporal jobs and auto-create Linear tickets. **Fix suggestion:** make `/api/swarm` fail-closed (require secret always in non-dev), or gate route registration on presence of `CM_HOOK_SECRET`.

[P1] `harness/views/swarm/dashboard.templ:145` (and similar new templ usages) - current `datastar.GetSSE(...)` generation is tripping `go test ./...` vet/build (`non-constant format string`), including `views/swarm/dashboard_templ.go:176`. **Impact:** branch does not pass Go test/build checks. **Fix suggestion:** update templ usage to pass a constant format string form expected by datastar helpers (or adjust helper usage pattern), regenerate templ files, and re-run `go test ./...`.

[P2] `harness/internal/db/queries/swarm.sql:130-132` - orphan cleanup compares RFC3339 text (`started_at` written as `time.RFC3339`) against `datetime('now', '-15 minutes')` format (`YYYY-MM-DD HH:MM:SS`). **Impact:** lexical comparison can misclassify stale spans (same-day entries may never be cleaned), leaving “running” spans orphaned. **Fix suggestion:** normalize both sides to the same format in SQL (e.g., wrap `started_at` with `datetime(started_at)`), and keep timestamp write format consistent.

[P2] `harness/agents/lib/codex-login.js:19-20` vs `harness/agents/lib/codex-auth.js:12-14` - login helper writes tokens to `~/.config/pi-codex/tokens.json`, but runtime auth loader only reads `~/.codex/auth.json`. **Impact:** running the provided login helper won’t authenticate agents, causing avoidable runtime failures. **Fix suggestion:** unify read/write location or have `codex-auth` fallback to the login helper’s path.

**Verdict: NEEDS CHANGES**.