---
description: Coordinate lead tasks through durable Pi q-manager children
argument-hint: <task-or-task-set>
---

Load and follow the `q-ceo` skill.

Manage this lead-engineer task set through durable `q-manager` children:

${ARGUMENTS:-Use the current user request as the task set.}
