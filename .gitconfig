[user]
  name = CoreyCole
  email = coreyleoc@gmail.com
  signingkey = 9508939C427DA750

[url "git@github.com:"]
  insteadOf = https://github.com/

[commit]
  gpgsign = true

[core]
  autocrlf = false
  attributesfile = ~/.gitattributes
  sshCommand = ssh -i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes
  editor = nvim
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  file-modified-label = "modified:"
  hunk-header-style = omit
  navigate = true  # use n and N to move between diff sections
  dark = true      # or light = true, or omit for auto-detection
  syntax-theme = "base16"
  # Terminals cannot do transparent ANSI backgrounds; use near-black red/green
  # backgrounds to approximate less distracting diff line highlights.
  plus-style = syntax "#001a00"
  plus-emph-style = syntax "#003000"
  minus-style = syntax "#1a0000"
  minus-emph-style = syntax "#300000"

[tag]
  gpgSign = true

[push]
  autoSetupRemote = true

[merge "mergiraf"]
  conflictStyle = zdiff3
  name = mergiraf
  driver = mergiraf merge --git %O %A %B -s %S -x %X -y %Y -p %P -l %L

[merge]
  conflictStyle = zdiff3

[pull]
  rebase = true
